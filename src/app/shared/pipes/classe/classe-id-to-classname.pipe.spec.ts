import { ClasseIdToClassnamePipe } from './classe-id-to-classname.pipe';

describe('ClasseIdToClassnamePipe', () => {
  it('create an instance', () => {
    const pipe = new ClasseIdToClassnamePipe();
    expect(pipe).toBeTruthy();
  });
});
